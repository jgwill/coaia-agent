# KINSHIP

* This here relates to using Hooking toward adequatly planning, hooking to events during sessions are advancing toward having what we want and also being capable to self-correct when claude is drifting from what is intended.
* There is so much that it is hard to fully document this here but that is a start.

## RELATIONSHIP with `/src/coaia-planning` and `/src/coaia-narrative`  that might be abstracted (programatically speaking into `/src/coaia-engine/KINSHIP.md`)

* I guess that LLm will document core relative element that can become a library or something common to what we are doing will go into `/src/coaia-engine/KINSHIP.md`


